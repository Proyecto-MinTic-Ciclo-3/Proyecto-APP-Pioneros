import React from 'react'

const PrivateLayout = ({children}) => {
    return (
        <div>
            esto es el privateLayout
            {children}
        </div>
    )
}

export default PrivateLayout
