import React from 'react'

const PublicLayout = ({children}) => {
    return (
        <div>
            Este es el layout pagina de inicio
            {children}
        </div>
    )
}

export default PublicLayout
